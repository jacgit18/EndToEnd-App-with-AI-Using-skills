# For the no-Docker path (plain pip + venv). The Docker build uses pyproject.toml via uv.
fastapi>=0.115
uvicorn[standard]>=0.32
sqlalchemy>=2.0
alembic>=1.14
psycopg[binary]>=3.2
pydantic>=2.10
pydantic-settings>=2.6
pyjwt>=2.10
passlib[bcrypt]>=1.7
python-multipart>=0.0.12

# dev
pytest>=8.3
httpx>=0.28
